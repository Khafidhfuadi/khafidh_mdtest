// app_colors.dart - Definisi warna yang digunakan di seluruh aplikasi
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background layers
  static const background = Color(0xFF0F172A);
  static const surface = Color(0xFF1E293B);
  static const surfaceVariant = Color(0xFF334155);

  // Accent
  static const primary = Color(0xFF6366F1);
  static const primaryLight = Color(0xFF818CF8);
  static const primarySplash = Color(0x266366F1);

  // Text
  static const textPrimary = Color(0xFFF8FAFC);
  static const textSecondary = Color(0xFF94A3B8);
  static const textHint = Color(0xFF475569);

  // Semantic
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);

  // Border
  static const border = Color(0xFF1E293B);
  static const borderLight = Color(0xFF334155);
}
